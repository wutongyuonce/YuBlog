---
title: Blogs
subtitle: Record my Trajectory
description: Browse all blog posts in a chronological list
bgType: rose
---

import BaseLayout from '~/layouts/BaseLayout.astro'
import StandardLayout from '~/layouts/StandardLayout.astro'
import ListView from '~/components/views/ListView.astro'

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
>
  <StandardLayout title={frontmatter.title} subtitle={frontmatter.subtitle}>
    <ListView collectionType="blogs" urlPath="blogs" />
  </StandardLayout>
</BaseLayout>
